node_modules
dist

# Capacitor: the iOS project is generated by `cap add ios` and refreshed by `cap sync`.
# Info.plist is edited by hand (landscape lock, hidden status bar), so the platform folder is
# NOT ignored wholesale - only the parts that are pure build output.
ios/App/App/public
ios/App/build
ios/App/Pods
ios/DerivedData
